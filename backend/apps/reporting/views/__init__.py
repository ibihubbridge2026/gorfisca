from .onboarding_views import OnboardingCompleteView, OnboardingStatusView

# ReportingViewSet est dans views.py (fichier principal), pas dans ce package
# Les imports depuis urls.py doivent utiliser: from apps.reporting import views as reporting_views
